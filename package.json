{
  "name": "guia-radioaficionado",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && rm -rf docs && cp -r dist docs"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
